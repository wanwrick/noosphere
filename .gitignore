# Claude Code: ignore session/cache; track skill + subagent definitions.
.claude/*
!.claude/skills/
!.claude/agents/

# DABs subproject local artifacts
templates/dabs-data-product-template/.databricks/
templates/dabs-data-product-template/dist/
templates/dabs-data-product-template/__pycache__/
templates/dabs-data-product-template/.venv/
templates/dabs-data-product-template/.pytest_cache/
**/*.pyc

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Environment files
.env
.env.local
.env.*.local

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Temp files
*.tmp
*.bak
*.orig

# Excalidraw diagrams (local review artifacts)
*.excalidraw

# Personal launch materials (not part of the public template)
linkedin/
canva/
github/
POSTING-GUIDE.md

# Sanitization lexicon — the REAL banned-token list. Never commit this to a
# public repo: it enumerates the employer / team / vendor / stakeholder proper
# nouns it exists to protect. Canonical copy lives in noosphere-private.
# Bootstrap with: cp .sanitization-lexicon.example .sanitization-lexicon.local
.sanitization-lexicon.local
